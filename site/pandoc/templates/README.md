# Pandoc Templates

This directory is reserved for explicit Pandoc templates when the defaults are not enough.

The current export layer intentionally uses small kind-aware defaults files instead of custom LaTeX or DOCX templates. That keeps PDF, EPUB, and DOCX generation close to Pandoc's stable defaults and avoids making any one PDF engine or reference document mandatory.
