# Example Series

Some writing belongs to a larger arc without needing an omnibus build on day one.

This example series page is a home for related works. It can describe the shape of the series, point readers at member publications, and eventually support richer series navigation without forcing every build to behave like a bound volume.

